React + Vite + TypeScript minimal frontend

Run locally:

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Start dev server:

```bash
npm run dev
```

The app will POST no-op commands to `http://localhost:8000/api/command` by default.
